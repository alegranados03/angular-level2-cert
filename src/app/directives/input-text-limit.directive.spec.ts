import { InputTextLimitDirective } from './input-text-limit.directive';

describe('InputTextLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTextLimitDirective();
    expect(directive).toBeTruthy();
  });
});
